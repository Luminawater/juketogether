# Cloudflare Pages SPA redirects
# Redirect all non-file routes to index.html for client-side routing
# Cloudflare Pages serves actual files (JS, CSS, images) automatically before applying redirects
/*  /index.html  200

