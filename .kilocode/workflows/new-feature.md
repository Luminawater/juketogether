# Setup New Feature

Follow these steps when creating a new feature:

1. Create feature branch from main
2. Create component file in `src/components/`
3. Add tests in same directory
4. Update documentation
5. Run tests with `npm test`
6. Submit PR for review

## Parameters needed:
- Feature name
- Component type (functional/class)
